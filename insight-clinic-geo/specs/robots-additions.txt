# AI crawler allowlist for insightclinic.care
# ---------------------------------------------------------------------------
# Shopify serves robots.txt from an editable template: robots.txt.liquid
# (Online Store > Themes > Edit code > templates/robots.txt.liquid).
# GOAL: make sure NONE of the AI answer-engine bots are blocked. Explicitly
# allowing them is belt-and-braces — the real risk is an accidental Disallow
# from an app, a password page, or a manual edit. VERIFY the live file first.
#
# In Shopify's Liquid template you can inject custom rules. A minimal, safe
# approach is to append explicit "allow" groups for the AI user-agents:

User-agent: GPTBot
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: ChatGPT-User
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Claude-User
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Perplexity-User
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: Applebot-Extended
Allow: /

User-agent: CCBot
Allow: /

User-agent: Amazonbot
Allow: /

# Point crawlers at the sitemap (Shopify auto-generates /sitemap.xml):
Sitemap: https://www.insightclinic.care/sitemap.xml

# ---------------------------------------------------------------------------
# NOTE: robots.txt only controls crawl access. It does NOT place structured
# data or content — that is pillars 1-3. And blocking a bot here does NOT
# remove you from a model's existing training data; it only prevents fresh
# real-time retrieval (which is exactly what Perplexity and AI Overviews use).
